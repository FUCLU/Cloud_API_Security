# TLS 1.3 only
ssl_protocols TLSv1.3;

# Tắt session tickets (chống replay TLS)
ssl_session_tickets off;

# Tắt 0-RTT (chống replay attack)
ssl_early_data off;

# Cipher suites TLS 1.3
ssl_ciphers TLS_AES_256_GCM_SHA384:TLS_AES_128_GCM_SHA256;