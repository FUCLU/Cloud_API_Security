_format_version: "3.0"

services:
  - name: backend-service
    url: http://backend:9000
    routes:
      - name: backend-route
        paths:
          - /

plugins:
  - name: opa-authz
    config:
      opa_url: "http://opa:8181/v1/data/authz"