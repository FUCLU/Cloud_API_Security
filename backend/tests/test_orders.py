"""Smoke tests for order security behavior.

These tests are intentionally lightweight placeholders until the Docker test
database fixture is wired. BOLA enforcement itself is tested at the helper level
in `app.security.bola_guard` and by `scripts/attacks/bola_attack.py`.
"""


def test_orders_test_suite_is_documented():
    assert True
