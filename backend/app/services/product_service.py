"""Product service helpers.

Product CRUD currently lives directly in `api.v1.products`. This module is kept
as the future business-logic boundary so the package tree documented in README
does not contain unexplained empty files.
"""
